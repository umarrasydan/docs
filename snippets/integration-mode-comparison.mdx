<div style={{
  maxWidth: '1200px',
  margin: '0 auto',
  padding: '24px',
  backgroundColor: '#ffffff',
  borderRadius: '12px',
  boxShadow: '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
  border: '1px solid #e5e7eb'
}}>
  <table style={{ width: '100%', borderCollapse: 'collapse' }}>
    <thead>
      <tr>
        <th style={{
          width: '50%',
          padding: '16px 24px',
          backgroundColor: '#ecfdf5',
          borderBottom: '2px solid #d1fae5',
          borderRight: '1px solid #d1fae5',
          textAlign: 'left'
        }}>
          <div style={{ fontSize: '18px', fontWeight: '700', color: '#047857', marginBottom: '4px' }}>Host-to-Host (H2H)</div>
          <div style={{ fontSize: '14px', color: '#0f766e' }}>Recommended for most merchants</div>
        </th>
        <th style={{
          width: '50%',
          padding: '16px 24px',
          backgroundColor: '#f9fafb',
          borderBottom: '2px solid #e5e7eb',
          textAlign: 'left'
        }}>
          <div style={{ fontSize: '18px', fontWeight: '700', color: '#374151', marginBottom: '4px' }}>Client-to-Client (C2C)</div>
          <div style={{ fontSize: '14px', color: '#6b7280' }}>For bespoke kiosk experiences</div>
        </th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td style={{ padding: '16px 24px', borderRight: '1px solid #ecfdf5', borderBottom: '1px solid #ecfdf5' }}>
          Managed orchestration with minimal custom code.
        </td>
        <td style={{ padding: '16px 24px', borderBottom: '1px solid #e5e7eb' }}>
          Full control of command flows, retries, and device telemetry.
        </td>
      </tr>
      <tr>
        <td style={{ padding: '16px 24px', borderRight: '1px solid #ecfdf5', borderBottom: '1px solid #ecfdf5' }}>
          Uses Terminal API sessions for payments, callbacks, and reconciliation.
        </td>
        <td style={{ padding: '16px 24px', borderBottom: '1px solid #e5e7eb' }}>
          Sends PAY/CANCEL/RECEIPT/SETTLEMENT commands directly from your app.
        </td>
      </tr>
      <tr>
        <td style={{ padding: '16px 24px', borderRight: '1px solid #ecfdf5', borderBottom: '1px solid #ecfdf5' }}>
          Xendit manages device orchestration and infrastructure.
        </td>
        <td style={{ padding: '16px 24px', borderBottom: '1px solid #e5e7eb' }}>
          Your team owns device connectivity, error handling, and security.
        </td>
      </tr>
      <tr>
        <td style={{ padding: '16px 24px', borderRight: '1px solid #ecfdf5', borderBottom: '1px solid #ecfdf5' }}>
          Rapid deployments for staffed checkout counters and mobile associates.
        </td>
        <td style={{ padding: '16px 24px', borderBottom: '1px solid #e5e7eb' }}>
          Ideal for unattended kiosks, experiential retail, or custom hardware setups.
        </td>
      </tr>
      <tr>
        <td style={{ padding: '16px 24px', borderRight: '1px solid #ecfdf5' }}>
          Works best when you need consistent UI with minimal engineering overhead.
        </td>
        <td style={{ padding: '16px 24px' }}>
          Best when you require fine-grained control over peripherals and offline logic.
        </td>
      </tr>
    </tbody>
  </table>

  <div style={{
    display: 'grid',
    gridTemplateColumns: '1fr 1fr',
    gap: '16px',
    marginTop: '32px'
  }}>
    <button
      onClick={() => {
        const integrationMode = document.getElementById('integration-mode-selection');
        const h2h = document.getElementById('h2h-quickstart');
        const h2hApp = document.getElementById('h2h-app-content');
        const c2c = document.getElementById('c2c-quickstart');
        const gatewayInfo = document.getElementById('gateway-app-info');

        const briSelected = document.getElementById('bri-checkbox')?.checked || false;
        const nttSelected = document.getElementById('ntt-checkbox')?.checked || false;
        const cashupSelected = document.getElementById('cashup-checkbox')?.checked || false;
        const shcSelected = document.getElementById('shc-checkbox')?.checked || false;

        if (integrationMode) integrationMode.style.display = 'none';
        if (c2c) c2c.style.display = 'none';

        if ((cashupSelected||shcSelected) && !briSelected && !nttSelected) {
          if (gatewayInfo) gatewayInfo.style.display = 'none';
          if (h2hApp) h2hApp.style.display = 'block';
          if (h2h) h2h.style.display = 'none';
          window.location.hash = 'h2h-app-content';
        } else {
          if (gatewayInfo) gatewayInfo.style.display = '';
          if (h2h) h2h.style.display = 'block';
          if (h2hApp) h2hApp.style.display = 'none';
          window.location.hash = 'h2h-quickstart';
        }
      }}
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        gap: '8px',
        padding: '16px',
        backgroundColor: '#047857',
        color: '#ffffff',
        fontWeight: '600',
        fontSize: '16px',
        borderRadius: '12px',
        border: 'none',
        cursor: 'pointer',
        boxShadow: '0 4px 12px 0 rgba(4, 120, 87, 0.35)'
      }}
    >
      <svg width="20" height="20" viewBox="0 0 20 20" fill="currentColor">
        <path fillRule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clipRule="evenodd" />
      </svg>
      Continue with H2H
    </button>

    <button
      onClick={() => {
        const integrationMode = document.getElementById('integration-mode-selection');
        const h2h = document.getElementById('h2h-quickstart');
        const c2c = document.getElementById('c2c-quickstart');
        const c2cApi = document.getElementById('c2c-api-content');
        const gatewaySetupList = document.getElementById('c2c-gateway-app-li');

        const briSelected = document.getElementById('bri-checkbox')?.checked || false;
        const nttSelected = document.getElementById('ntt-checkbox')?.checked || false;
        const cashupSelected = document.getElementById('cashup-checkbox')?.checked || false;
        const shcSelected = document.getElementById('shc-checkbox')?.checked || false;

        if (integrationMode) integrationMode.style.display = 'none';
        if (h2h) h2h.style.display = 'none';

        if ((cashupSelected||shcSelected) && !briSelected && !nttSelected) {
          if (gatewaySetupList) gatewaySetupList.style.display = 'none';
        } else {
          if (gatewaySetupList) gatewaySetupList.style.display = 'block';
        }

        if (c2cApi) c2cApi.style.display = 'none';
        if (c2c) c2c.style.display = 'block';
        if (window.updateC2CProviderSnippets) {
          window.updateC2CProviderSnippets();
        }
        window.location.hash = 'c2c-quickstart';
      }}
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        gap: '8px',
        padding: '16px',
        backgroundColor: '#4b5563',
        color: '#ffffff',
        fontWeight: '600',
        fontSize: '16px',
        borderRadius: '12px',
        border: 'none',
        cursor: 'pointer'
      }}
    >
      <svg width="20" height="20" viewBox="0 0 20 20" fill="currentColor">
        <path fillRule="evenodd" d="M11.49 3.17c-.38-1.56-2.6-1.56-2.98 0a1.532 1.532 0 01-2.286.948c-1.372-.836-2.942.734-2.106 2.106.54.886.061 2.042-.947 2.287-1.561.379-1.561 2.6 0 2.978a1.532 1.532 0 01.947 2.287c-.836 1.372.734 2.942 2.106 2.106a1.532 1.532 0 012.287.947c.379 1.561 2.6 1.561 2.978 0a1.533 1.533 0 012.287-.947c1.372.836 2.942-.734 2.106-2.106a1.533 1.533 0 01.947-2.287c1.561-.379 1.561-2.6 0-2.978a1.532 1.532 0 01-.947-2.287c.836-1.372-.734-2.942-2.106-2.106a1.532 1.532 0 01-2.287-.947zM10 13a3 3 0 100-6 3 3 0 000 6z" clipRule="evenodd" />
      </svg>
      Continue with C2C
    </button>
  </div>

  <Info>
  Ready to start? H2H fits most deployments for its simplicity and speed. Explore the [H2H documentation](/sdk/h2h/introduction) or [C2C documentation](/sdk/c2c/introduction) for deeper implementation details.
  </Info>
</div>